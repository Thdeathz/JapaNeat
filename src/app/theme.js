import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#fffffe'
    },
    secondary: {
      main: '#90b4ce'
    },
    secondaryBtn: {
      main: '#3da9fc'
    },
    textDefault: {
      main: '#094067'
    },
    navBackground: {
      main: '#bae8e8'
    }
  }
})
