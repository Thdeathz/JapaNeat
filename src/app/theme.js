import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#fffffe'
    },
    secondary: {
      main: '#90b4ce'
    },
    textDefault: {
      main: '#094067'
    },
    navBackground: {
      main: '#bae8e8'
    }
  }
})
